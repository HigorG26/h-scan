# Allowlist de alvos autorizados a serem varridos.
# Suporta wildcards (fnmatch). Só adicione hosts para os quais você tem
# autorização formal de teste (laboratório próprio, pentest contratado, etc).

allowed_hosts:
  - "app-teste.local"
  - "*.lab.internal"
  - "juice-shop"          # container OWASP Juice Shop (lab de teste)
  - "localhost"
  # - "meudominio-de-teste.com"
